local wezterm = require("wezterm")
local act = wezterm.action

local dracula = {
	background = "#1A1A2E", -- Deep Space Blue
	foreground = "#5A9CA6", -- Light Lavender (Pastel)
	cursor_bg = "#6272a4", -- Neon Pink (Cyberpunk)
	cursor_border = "#F0E6F6", -- Light Lavender (Pastel)
	cursor_fg = "#111A25", -- Deep Space Blue
	selection_bg = "#3E3E5E", -- Muted Indigo
	selection_fg = "#C0E6F6", -- Light Lavender (Pastel)
	ansi = {
		"#2E2E3E", -- Muted Dark Blue
		"#D77FD7", -- Muted Pink
		"#3E3E5E", -- Muted Indigo
		"#A77FA7", -- Muted Soft Pink
		"#00BFBF", -- Muted Cyan
		"#A71585", -- Muted Purple
		"#2FFF14", -- Muted Green
		"#D0D6D6", -- Muted Lavender
	},
	brights = {
		"#5A5A6A", -- Muted Gray
		"#D77FD7", -- Muted Pink
		"#7FBF7F", -- Muted Green
		"#D7B9A9", -- Muted Peach
		"#00AFAF", -- Muted Cyan
		"#A71585", -- Muted Purple
		"#2FFF14", -- Muted Green
		"#E0E0E0", -- Muted White
	},
}

return {
	launch_menu = {
		{
			label = "Windows PowerShell",
			args = { "pwsh" },
		},
	},

	max_fps = 120,
	animation_fps = 120,

	default_prog = { "WSL" },
	hide_tab_bar_if_only_one_tab = true,

	colors = {
		background = dracula.background,
		foreground = dracula.foreground,
		cursor_bg = dracula.cursor_bg,
		cursor_border = dracula.cursor_border,
		cursor_fg = dracula.cursor_fg,
		selection_bg = dracula.selection_bg,
		selection_fg = dracula.selection_fg,

		ansi = dracula.ansi,
		brights = dracula.brights,
	},

	freetype_load_flags = "NO_HINTING",
	font = wezterm.font_with_fallback({
		{ family = "FiraCode Nerd Font", weight = "Medium" },
		{ family = "JetBrains Mono", weight = "Medium" },
		{ family = "CaskaydiaCove Nerd Font", weight = "Bold" },
	}),
	font_size = 18.0,

	keys = {
		{ key = "v", mods = "CTRL|SHIFT", action = act({ PasteFrom = "Clipboard" }) },
		{ key = "c", mods = "CTRL|SHIFT", action = act({ CopyTo = "Clipboard" }) },
		{ key = "l", mods = "CTRL|SHIFT", action = act.ShowLauncher },
	},

	window_decorations = "RESIZE",
	window_frame = {
		font = wezterm.font("FiraCode Nerd Font Mono", { weight = "Medium" }),
	},
	background = {
		{
			source = {
				File = "C:\\Users\\bstrang\\Pictures\\bluesquares1.jpg",
			},
			hsb = { brightness = 0.03, saturation = 0.8, hue = 0.8 },
		},
	},
}
